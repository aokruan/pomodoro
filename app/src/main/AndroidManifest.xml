<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.aokruan.pomodoro">

    <application
        android:name=".PomodoroApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_tomato"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_tomato"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pomodoro">
        <activity
            android:name=".ru.aokruan.pomodoro.core.presentation.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Pomodoro.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>